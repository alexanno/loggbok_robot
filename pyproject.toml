[project]
name = "loggbok-robot"
version = "0.1.0"
description = "Generate creative ship log entries using weather and location data with Ollama"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "ollama>=0.1.0",
]
